/**
 * Deprecated SPA entrypoint.
 *
 * The application now runs in React Router Framework Mode (SSR),
 * using `app/entry.client.tsx` and `app/entry.server.tsx`.
 *
 * This file is intentionally left empty to prevent accidental
 * client-side mounting via createRoot.
 *
 * Safe to delete once all references are removed.
 */
export {};
